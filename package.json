{
  "name": "electron-app-base",
  "version": "1.0.0",
  "description": "Aplicativo base de electron",
  "main": "index.js",
  "scripts": {
    "dev": "cross-env NODE_ENV=development electron src/index.js",
    "lint": "standard",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cross-env": "^7.0.3",
    "electron-compile": "^6.4.4",
    "electron-debug": "^3.2.0"
  },
  "devDependencies": {
    "devtron": "^1.4.0",
    "electron-prebuilt-compile": "^8.2.0",
    "standard": "^16.0.4"
  }
}
